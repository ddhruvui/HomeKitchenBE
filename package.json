{
  "name": "home-kitchen",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared",
    "api"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w api",
    "test": "npm test -w shared && npm test -w api",
    "dev": "npm run dev -w api",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w api"
  }
}
